{
  "name": "pacelized-agcs",
  "version": "1.0.0",
  "description": "NIRSAL Parcelized Agcs API",
  "main": "app.js",
  "scripts": {
    "exportdb": "cd ./data && node export-fs-data-to-db.js --export",
    "deploy": "git push && git push heroku master && heroku open",
    "deploy:default": "git add --all && git commit -m 'Housekeeping' && git push && git push heroku master && heroku open",
    "start": "node server.js",
    "start:dev": "nodemon server.js",
    "start:prod": "NODE_ENV=production nodemon server.js"
  },
  "author": "Nduka Okpue",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.20.0",
    "body-parser": "^1.19.0",
    "chalk": "^4.1.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.19",
    "morgan": "^1.10.0",
    "pug": "^3.0.0",
    "request": "^2.88.2",
    "validator": "^13.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "engines": {
    "node": "^12"
  }
}
